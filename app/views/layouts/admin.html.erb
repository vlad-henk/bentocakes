<!DOCTYPE html>
<html>
<head>
  <title>Адмінпанель | BentoCakes</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-100">
  <!-- Навігація адмінки -->
  <nav class="bg-rose-800 text-white shadow-lg">
    <div class="container mx-auto px-4">
      <div class="flex justify-between items-center py-4">
        <div class="flex items-center space-x-4">
          <%= link_to 'BentoCakes Admin', admin_root_path, class: 'text-xl font-bold' %>
          <%= link_to 'Головна сайту', root_path, class: 'text-rose-200 hover:text-white' %>
        </div>
        
        <div class="flex items-center space-x-6">
          <%= link_to 'Товари', admin_products_path, 
                class: "#{'bg-rose-700' if controller_name == 'products'} px-4 py-2 rounded hover:bg-rose-700" %>
          <%= link_to 'Замовлення', admin_orders_path, 
                class: "#{'bg-rose-700' if controller_name == 'orders'} px-4 py-2 rounded hover:bg-rose-700" %>
          <%= link_to 'Статистика', admin_root_path, 
                class: "#{'bg-rose-700' if controller_name == 'dashboard'} px-4 py-2 rounded hover:bg-rose-700" %>
          
          <div class="flex items-center space-x-2">
            <span class="text-rose-200"><%= current_user.email %></span>
            <%= link_to 'Вийти', destroy_user_session_path, method: :delete, 
                  class: 'bg-rose-700 px-4 py-2 rounded hover:bg-rose-600' %>
          </div>
        </div>
      </div>
    </div>
  </nav>
  
  <!-- Flash повідомлення -->
  <% if notice %>
    <div class="bg-green-500 text-white px-4 py-2 text-center">
      <%= notice %>
    </div>
  <% end %>
  
  <% if alert %>
    <div class="bg-red-500 text-white px-4 py-2 text-center">
      <%= alert %>
    </div>
  <% end %>
  
  <!-- Основний контент -->
  <main class="container mx-auto px-4 py-8">
    <%= yield %>
  </main>
</body>
</html>