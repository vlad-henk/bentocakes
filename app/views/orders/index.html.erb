<%# app/views/orders/index.html.erb %>
<div class="min-h-screen w-full bg-gradient-to-b from-pink-50 to-white">
  <%= render 'shared/navbar' %>
  
  <div class="container mx-auto px-4 py-8">
    <!-- Хлібні крихти -->
    <nav class="flex space-x-2 text-sm text-rose-600 mb-8">
      <%= link_to 'Головна', root_path, class: 'hover:text-rose-800' %>
      <span>/</span>
      <span class="text-rose-900 font-medium">Мої замовлення</span>
    </nav>
    
    <h1 class="text-4xl font-bold text-rose-900 mb-8">Мої замовлення</h1>
    
    <% if @orders.any? %>
      <div class="space-y-6">
        <% @orders.each do |order| %>
          <div class="bg-white rounded-3xl shadow-lg overflow-hidden hover:shadow-xl transition">
            <div class="p-6">
              <div class="flex flex-col md:flex-row md:items-center justify-between mb-4">
                <div>
                  <h3 class="text-xl font-bold text-rose-900">Замовлення #<%= order.id %></h3>
                  <p class="text-rose-600 text-sm mt-1">
                    <%= format_date(order.created_at) %>
                  </p>
                </div>
                
                <div class="flex items-center space-x-4 mt-4 md:mt-0">
                  <!-- Статус - використовуємо helper метод -->
                  <span class="px-4 py-2 rounded-full text-sm font-medium <%= status_classes(order.status) %>">
                    <%= order_status_text(order.status) %>
                  </span>
                  
                  <!-- Сума -->
                  <span class="text-2xl font-bold text-amber-600">
                    <%= number_to_currency(order.total, unit: '₴', format: '%n %u') %>
                  </span>
                </div>
              </div>
              
              <!-- Деталі замовлення -->
              <div class="grid md:grid-cols-3 gap-4 mb-4">
                <div class="bg-rose-50 rounded-xl p-4">
                  <h4 class="font-bold text-rose-800 mb-2">Адреса доставки</h4>
                  <p class="text-rose-700"><%= order.delivery_address %></p>
                </div>
                
                <div class="bg-rose-50 rounded-xl p-4">
                  <h4 class="font-bold text-rose-800 mb-2">Контактний телефон</h4>
                  <p class="text-rose-700"><%= order.phone %></p>
                </div>
              
              </div>
              
              <!-- Кількість товарів -->
              <div class="flex justify-between items-center">
                <div class="text-rose-700">
                  <span class="font-medium"><%= order.items_count %> товарів</span>
                </div>
                
                <%= link_to 'Переглянути деталі', order_path(order),
                      class: 'bg-rose-100 text-rose-700 px-6 py-2 rounded-full hover:bg-rose-200 transition font-medium' %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <!-- Якщо замовлень немає -->
      <div class="text-center py-16">
        <div class="text-8xl mb-6">📦</div>
        <h2 class="text-3xl font-bold text-rose-900 mb-4">Замовлень ще немає</h2>
        <p class="text-rose-700 mb-8 max-w-md mx-auto">
          Ви ще не робили замовлень. Почніть з вибору улюблених солодощів!
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <%= link_to 'Переглянути меню', products_path,
                class: 'bg-gradient-to-r from-rose-500 to-pink-600 text-white font-bold py-3 px-8 rounded-full hover:shadow-xl transition' %>
          <%= link_to 'Повернутися на головну', root_path,
                class: 'border-2 border-rose-600 text-rose-600 font-bold py-3 px-8 rounded-full hover:bg-rose-50 transition' %>
        </div>
      </div>
    <% end %>
  </div>
  
  <%= render 'shared/footer' %>
</div>