<div class="min-h-screen w-full bg-gradient-to-b from-pink-50 to-white">
  <%= render 'shared/navbar' %>
  
  <div class="container mx-auto px-4 py-8">
    <!-- Хлібні крихти -->
    <nav class="flex space-x-2 text-sm text-rose-600 mb-8">
      <%= link_to 'Головна', root_path, class: 'hover:text-rose-800' %>
      <span>/</span>
      <%= link_to 'Кошик', cart_path, class: 'hover:text-rose-800' %>
      <span>/</span>
      <span class="text-rose-900 font-medium">Оформлення замовлення</span>
    </nav>
    
    <h1 class="text-4xl font-bold text-rose-900 mb-8">Оформлення замовлення</h1>
    
    <% if @cart.cart_items.empty? %>
      <div class="text-center py-16">
        <div class="text-8xl mb-6">🛒</div>
        <h2 class="text-3xl font-bold text-rose-900 mb-4">Кошик порожній</h2>
        <p class="text-rose-700 mb-8">Додайте товари до кошика перед оформленням замовлення</p>
        <%= link_to 'Перейти до меню', products_path,
              class: 'bg-gradient-to-r from-rose-500 to-pink-600 text-white font-bold py-3 px-8 rounded-full hover:shadow-xl transition' %>
      </div>
    <% else %>
      <%= form_with(model: @order, url: orders_path, local: true, class: 'space-y-8') do |f| %>
        <div class="grid lg:grid-cols-3 gap-8">
          <!-- Форма введення даних -->
          <div class="lg:col-span-2 space-y-8">
            <!-- Контактна інформація -->
            <div class="bg-white rounded-3xl shadow-xl p-8">
              <h2 class="text-2xl font-bold text-rose-900 mb-6">Контактна інформація</h2>
              
              <div class="space-y-6">
                <div>
                  <%= f.label :phone, "Телефон *", class: 'block text-rose-700 mb-2 font-medium' %>
                  <%= f.telephone_field :phone, required: true,
                        class: 'w-full border border-rose-200 rounded-xl p-3 focus:border-rose-300 focus:ring focus:ring-rose-100 transition',
                        placeholder: '+38 (0XX) XXX XX XX' %>
                </div>
                
                <div>
                  <%= f.label :delivery_address, "Адреса доставки *", class: 'block text-rose-700 mb-2 font-medium' %>
                  <%= f.text_area :delivery_address, required: true, rows: 3,
                        class: 'w-full border border-rose-200 rounded-xl p-3 focus:border-rose-300 focus:ring focus:ring-rose-100 transition',
                        placeholder: 'Введіть повну адресу доставки' %>
                </div>
              </div>
            </div>
            
            <!-- Спосіб оплати -->
            <!--<div class="bg-white rounded-3xl shadow-xl p-8">
              <h2 class="text-2xl font-bold text-rose-900 mb-6">Спосіб оплати</h2>
              
              <div class="grid md:grid-cols-2 gap-4">
                <div>
                  <%= f.radio_button :payment_method, 'card', class: 'hidden peer', checked: true %>
                  <%= f.label :payment_method_card, 
                        class: 'block p-4 border-2 border-rose-200 rounded-xl cursor-pointer peer-checked:border-rose-500 peer-checked:bg-rose-50 hover:bg-rose-50 transition' do %>
                    <div class="flex items-center">
                      <div class="w-6 h-6 border-2 border-rose-300 rounded-full mr-3 flex items-center justify-center">
                        <div class="w-3 h-3 bg-rose-500 rounded-full hidden peer-checked:block"></div>
                      </div>
                      <div>
                        <div class="font-medium text-rose-900">Картка онлайн</div>
                        <div class="text-sm text-rose-600">Безпечна оплата</div>
                      </div>
                    </div>
                  <% end %>
                </div>
                
                <div>
                  <%= f.radio_button :payment_method, 'cash', class: 'hidden peer' %>
                  <%= f.label :payment_method_cash,
                        class: 'block p-4 border-2 border-rose-200 rounded-xl cursor-pointer peer-checked:border-rose-500 peer-checked:bg-rose-50 hover:bg-rose-50 transition' do %>
                    <div class="flex items-center">
                      <div class="w-6 h-6 border-2 border-rose-300 rounded-full mr-3 flex items-center justify-center">
                        <div class="w-3 h-3 bg-rose-500 rounded-full hidden peer-checked:block"></div>
                      </div>
                      <div>
                        <div class="font-medium text-rose-900">Готівка</div>
                        <div class="text-sm text-rose-600">Кур'єру або в касі</div>
                      </div>
                    </div>
                  <% end %>
                </div>
              </div>
            </div> -->
            
            <!-- Примітки -->
            <div class="bg-white rounded-3xl shadow-xl p-8">
              <h2 class="text-2xl font-bold text-rose-900 mb-6">Додаткові побажання</h2>
              <%= f.text_area :notes, rows: 4,
                    class: 'w-full border border-rose-200 rounded-xl p-3 focus:border-rose-300 focus:ring focus:ring-rose-100 transition',
                    placeholder: 'Ваші побажання щодо замовлення...' %>
            </div>
          </div>
          
          <!-- Підсумок замовлення -->
<!-- Список товарів -->
            <div class="space-y-4 mb-6">
              <% @cart.cart_items.each do |item| %>
                <div class="flex items-center justify-between">
                  <div class="flex items-center">
                    <div class="w-12 h-12 bg-rose-100 rounded-lg flex items-center justify-center mr-3">
                      <span class="text-lg">
                        <% 
                          emoji = case item.product.category
                          when 'cakes'
                            '🎂'
                          when 'cupcakes'
                            '🧁'
                          when 'macarons'
                            '🍪'
                          else
                            '🍰'
                          end
                        %>
                        <%= emoji %>
                      </span>
                    </div>
                    <div>
                      <div class="font-medium text-rose-900 text-sm"><%= item.product.name.truncate(20) %></div>
                      <div class="text-rose-600 text-xs"><%= item.quantity %> шт.</div>
                    </div>
                  </div>
                  <span class="font-medium text-rose-900">
                    <%= number_to_currency(item.total_price, unit: '₴', format: '%n %u') %>
                  </span>
                </div>
              <% end %>
            </div>
              
              <!-- Кнопка підтвердження -->
              <div class="mt-8">
                <%= f.submit 'Підтвердити замовлення',
                      class: 'w-full bg-gradient-to-r from-rose-500 to-pink-600 text-white font-bold py-4 rounded-xl hover:shadow-xl transition shadow-lg cursor-pointer text-lg' %>
              </div>
              
              <!-- Умови -->
              <div class="mt-6 text-center">
                <p class="text-xs text-rose-500">
                  Натискаючи кнопку, ви погоджуєтесь з 
                  <%= link_to 'умовами обробки персональних даних', '#', class: 'text-rose-600 hover:text-rose-800' %>
                </p>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
  
  <%= render 'shared/footer' %>
</div>